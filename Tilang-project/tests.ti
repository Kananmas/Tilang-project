var int age = 10;
var bool virgin = true;
const string name = "kanan";
var int[] numbers = [1,2,3,4,5];
var int[][] nextNumbers = [numbers , [6,7,8,9,10]];


type Address {
	string City;
	string PostCode;

	function concatAddress() <string> {
		return City + " " + PostCode;
	}
}

type Person {
	string name;
	int age;
	Address address;
};

var Person person = Person {
	name = "kanan",
	age = 10 +12
};

function Introduce() <void> {
	Sys.out.println(name + " " + age);
}

function Sum(a:int , b:int) <int> {
	return a + b;
}

person = Person {
	name = "hasan",
	age = 33,
	address = Address {
		City = "tehran",
		PostCode = "1231231",
	},
};


Sys.out.println(getCharCode('e') + nextNumbers[0][4] + Sum(Sum(9,Sum(3,nextNumbers[0][2])),8/2));
